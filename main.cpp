#include "game.h"

int main()
{
    /*f f;
    Transition tr(TransitionContract(f, 0, std::chrono::seconds(10), {10.f}));
    tr.setElapsed(std::chrono::seconds(2));*/
    game();

    return 0;
} // main()
